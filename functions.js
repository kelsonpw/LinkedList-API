// const applications = async function() {
//   const jobs = await db.query('SELECT * FROM jobs_users WHERE user_id=$1', [
//     data.rows[0].id
//   ]);
//   jobIds = jobs.rows.map(job => job.job_id);
//   data.rows[0].applied_to = jobIds;
// };

// module.exports = applications;
